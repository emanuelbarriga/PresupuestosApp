# OpenSpec Configuration — gestor-de-presupuestos (presupuestosapp)
# Auto-generated by sdd-init. Do not edit manually — regenerate via sdd-init.

context: |
  Budget management app for Colombian SMEs (Pácora/Samán). Tracks ingresos/egresos
  by project, client, and month with budgeted vs executed amounts. Firebase Firestore
  as primary data layer with real-time subscriptions. Next.js 15 App Router + React 19.
  UI in Spanish, COP currency. No auth layer yet — single-tenant.

strict_tdd: true

testing:
  runner: vitest
  runner_version: 4.1.9
  command: npm test
  layers:
    unit: { available: true, tool: vitest + vi.mock }
    integration: { available: true, tool: '@testing-library/react + jsdom' }
    e2e: { available: false, tool: null }
  coverage:
    available: true
    command: npx vitest --coverage
  quality:
    linter: { available: true, command: npm run lint }
    type_checker: { available: true, command: npx tsc --noEmit }
    formatter: { available: false, command: null }

phases:
  propose: { rules: [] }
  spec: { rules: [] }
  design: { rules: [] }
  tasks: { rules: [] }
  apply: { rules: [] }
  verify: { rules: [] }
  archive:
    rules: []
    notes: |

archived_changes:
  - name: company-context-zustand
    archived_at: 2026-07-13
    summary: "Zustand 5.0.14 dual-write migration: created companyStore.ts (53 lines, 7 actions, 17 tests), migrated Sidebar/InvitacionEntity/ColaboradorEntity to selector-based subscriptions, 606/606 tests passing, tsc zero new errors"
    main_spec: ~
  - name: sidepanel-entity-unification
    archived_at: 2026-07-09
    summary: Unificó 10 entidades en componentes únicos create|edit|view, reemplazó FormPanel/ViewPanel/DataPanel, 29/29 tareas completadas, cutover sin regresiones
    main_spec: openspec/specs/sidepanel-entity-components/spec.md
  - name: critical-technical-debt-fixes
    archived_at: 2026-07-09
    summary: Fixed cross-tenant subscription (H4), consolidated entity-grouping into shared utility (H5), replaced native browser dialogs with react-hot-toast (H6) — 17/17 tasks, zero regressions
  - name: date-fix
    archived_at: 2026-07-01
    summary: Added fechaPresupuestado (YYYY-MM) to Budget type for multi-year support
    main_spec: openspec/specs/budget-date/spec.md
  - name: sidepanel-tests
    archived_at: 2026-07-01
    summary: Added 37 unit tests for Sidepanel component (8 sub-components), Dashboard data construction, and form logic
    main_spec: openspec/specs/sidepanel-testing/spec.md
  - name: defensive-dates-configuracion
    archived_at: 2026-07-10
    summary: Extracted toMillis helper from aggregatedInvitations.useMemo and applied defensive date handling in getInvitationStatus and days-remaining render. 5/5 tests pass, tsc sin errores nuevos.
    main_spec: ~
  - name: batch-accept-invitations
    archived_at: 2026-07-10
    summary: Al aceptar una invitación, el endpoint batch-acepta todas las invitaciones pendientes del mismo email en un solo WriteBatch atómico. 4/4 tareas completadas, tsc sin errores nuevos.
  - name: nuevo-flujo-invitacion-registro
    archived_at: 2026-07-10
    summary: "Separó registro de membresía: invitación solo con email, registro manual, post-registro redirige a pending-assignment, admin asigna empresa/rol desde Configuración. 26/26 tareas completadas. Refactor puro de modelo y flujo, sin spec changes."
  - name: data-date-validation
    archived_at: 2026-07-13
    summary: "Zod 4.4.3 date validation at write boundary: created lib/schemas.ts with yearMonthSchema/dateStringSchema/budgetSchema/ejecucionSchema, 39 tests, validated 4 firestore.ts write functions, integrated in BudgetForm/EjecucionForm/ConvertirMovimientosEntity, 645/645 tests passing, 0 tsc errors. Pure refactor — no spec changes."
  - name: er-config-system
    archived_at: 2026-07-13
    summary: "Per-company ER config system: ErConfig type + Firestore subcollection, dual tax regime (simple 8.1% / común 35%), refactored computePnL with config-driven formulas, ErConfigPanel with regime selector + per-line formula config, wired into Sidepanel and page.tsx. 12/13 tasks complete, 650/650 tests passing, 0 tsc errors."
  - name: bulk-edit-cascade
    archived_at: 2026-07-13
    summary: "Checkbox + floating action bar + BulkEditPresupuestosPanel/BulkEditEjecucionesPanel for batch field updates on Presupuestos and Ejecuciones tabs; cascadeTerceroName propagates name changes to entityName on linked budgets/ejeuciones; batchUpdatePresupuestos/batchUpdateEjecuciones in firestore.ts. 13/13 tasks complete, 683/683 tests passing, 0 tsc errors."
    main_spec: openspec/specs/bulk-edit-cascade/spec.md
  - name: soportes-entidad
    archived_at: 2026-07-14
    summary: "Soportes tab in TerceroView and ProjectView: shared SoportesTab component with entity-filtered subscribeDocumentos (terceroId/projectId), tab bar matching MediaPage style, card-based doc list with loading/empty states. 7/7 tasks complete, 812 tests passing, 0 tsc errors."
    main_spec: openspec/specs/sidepanel-entity-components/spec.md
  - name: sistema-medios-desacoplado
    archived_at: 2026-07-14
    summary: "Decoupled media system: DocumentoMedio type + /documentos collection, Inbox (bulk-upload dropzone + grid), DocumentoSidepanel (preview + classification), flat Storage paths, EjecucionForm persists to /documentos, mediaService/mediaLinking services, firestore.rules + storage.rules for documentos, migration/GC scripts. 27/27 tasks complete, 783 tests passing, 0 tsc errors."
    main_spec: openspec/specs/comprobantes-ejecucion/spec.md
  - name: deuda-tecnica-pdf-transactions
    archived_at: 2026-07-14
    summary: "Hybrid transaction fix (6 atomicity gaps) + PDF canvas preview (3 components, shared PdfViewer). 14/14 tasks, 820 tests passing, 0 regressions."
    main_spec: openspec/specs/data-atomicity/spec.md
  - name: ocr-gemini-integration
    archived_at: 2026-07-16
    summary: "OCR extraction via Gemini 2.5 Flash: POST /api/ocr/extract route with auth, 5MB guard, 429 retry, structured nullable output; DocumentoSidepanel 'Extraer con IA' button with non-destructive pre-fill, 30s timeout, error mapping; new ocr-extraction spec created, document-classification spec updated. 846/846 tests passing, 0 regressions."
    main_spec: openspec/specs/ocr-extraction/spec.md
  - name: batch-ocr
    archived_at: 2026-07-16
    summary: "Batch multi-document OCR from Inbox: checkbox selection (max 30), floating action bar, 3-parallel client-orchestrated extraction via existing /api/ocr/extract, per-doc progress overlays via useReducer, non-destructive individual writes via updateDocumentoMedio, 429 retry with backoff, AbortController cancel, lib/ocr.ts extracted. 885/885 tests passing, 0 tsc errors."
    main_spec: openspec/specs/batch-ocr/spec.md
